import { Button } from '../ui/button'

export default function RequestACallButton() {
  return (
    <Button
      size="sm"
      className="rounded-full text-xs md:text-sm px-4 py-0 mb-1 cursor-pointer"
    >
      Request A Call
    </Button>
  )
}
